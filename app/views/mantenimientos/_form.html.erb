<%= form_for(@mantenimiento) do |f| %>
	<% if @mantenimiento.errors.any? %>
	<div id="error_explanation">
	  <h2><%= pluralize(@mantenimiento.errors.count, "error") %> prohibited this mantenimiento from being saved:</h2>

		<ul>
		<% @mantenimiento.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
		</ul>
	</div>
	<% end %>

	<div class="field">
	<%= f.label :fecha %><br>
	<%= f.date_select :fecha %>
	</div>
	<div class="field">
	<%= f.label :observacion %><br>
	<%= f.text_area :observacion %>
	</div>
	<div class="field">
	<%= f.label :kilometraje %><br>
	<%= f.number_field :kilometraje %>
	</div>
	<div class="field">
	<%= f.label :costo %><br>
	<%= f.text_field :costo %>
	</div>
	<div classs="autobuses">
	<%= collection_select(:autobus, :collection_select, Autobus.all, :id, :modelo) %>
	</div>
	<div class="actions">
	<%= f.submit %>
	</div>
<% end %>
